export default class User {
  constructor(name, birthdate, username, password) {
    this.name = name;
    this.birthdate = birthdate;
    this.username = username;
    this.password = password;
  }
}

